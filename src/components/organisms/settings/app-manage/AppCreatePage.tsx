import {FC} from 'react';
import AppCreateForm from './AppCreateForm.client';
import {Stack} from '@mui/material';

interface Props {}

const AppCreatePage: FC<Props> = () => {
  return <AppCreateForm />;
};

export default AppCreatePage;
